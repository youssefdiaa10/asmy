export function greet(name: string): string {
  return `Hi, I am ${name}`;
}

export function motivate(name: string): string {
  return `You can do it ${name}. I believe in you.`;
}

export function love(name: string): string {
  return `I love ${name} 💘.`;
}
